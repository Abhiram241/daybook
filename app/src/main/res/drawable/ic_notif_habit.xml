<!--
  v0.5.1 §F-drawables — per-category notification small icon: HABIT (a check / tick).
  Do NOT rename: NotificationUtils.smallIconFor() references R.drawable.ic_notif_habit.

  Constraints (see ic_notification.xml's header):
    - a small icon SystemUI cannot inflate makes the WHOLE notification vanish, silently;
    - flat white silhouette only: android:fillColor="#FFFFFFFF" on a solid closed path,
      NO strokeColor / strokeWidth;
    - 24dp x 24dp, viewport 24x24, glyph inset from every edge (some OEMs circle-mask);
    - no degenerate near-zero-length arcs. This path is a single closed hexagon, no arcs.
  NavIconInflateTest.notifHabit() inflates this against a real Resources.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp" android:height="24dp"
    android:viewportWidth="24" android:viewportHeight="24">
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M9.75,15.05L18.05,6.75L19.6,8.3L9.75,18.15L4.4,12.8L5.95,11.25Z" />
</vector>
